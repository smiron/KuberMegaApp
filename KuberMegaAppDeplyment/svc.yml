apiVersion: v1
kind: Service
metadata:
  name: theapp-svc
  labels:
    app: theapp
    zone: dev
    version: 1.0.0
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30001
    protocol: TCP
  selector:
    app: theapp
    zone: dev
    version: 1.0.0